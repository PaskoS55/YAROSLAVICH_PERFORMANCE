{
  "name": "@pasko-performance/license-issuer",
  "version": "1.0.0",
  "productName": "PASKO License Issuer",
  "description": "Private offline operator tool for PASKO Performance licenses",
  "author": "Sergey Pasko",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src scripts test forge.config.cjs",
    "test:run": "npm run build && node --test test/*.test.mjs",
    "dev": "npm run build && electron-forge start",
    "package": "npm run build && electron-forge package --platform win32 --arch x64",
    "make": "npm run build && electron-forge make --platform win32 --arch x64",
    "release": "node scripts/release.mjs",
    "release:verify": "node scripts/verify-release.mjs"
  },
  "devDependencies": {
    "@electron-forge/cli": "7.11.2",
    "@electron-forge/maker-squirrel": "7.11.2",
    "electron": "43.4.1"
  }
}
